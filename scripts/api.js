// scripts/api.js
// Supabase 클라이언트 초기화
// 실제 배포 환경에서는 환경변수(SUPABASE_URL, SUPABASE_KEY)를 사용하세요.

// 예시:
// import { createClient } from '@supabase/supabase-js';
// export const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_KEY); 